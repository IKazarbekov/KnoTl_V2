# KnoTl V2

Веб-приложение для управления заметками (или что делает проект, кратко).  
Создано в рамках изучения Python и backend-разработки.

## Технологии
- Python 3.11+
- Flask
- Flask-SQLAlchemy
- PostgreSQL (или SQLite)
- Docker
- Pytest

## Функциональность
- Регистрация и аутентификация пользователей
- Создание, удаление задач
- Переводчик с русским и английским языков(использует api)
- Онлайн игра крестики-нолики
- Общий чат

## Запуск через Docker
1. Убедитесь, что установлен Docker и Docker Compose.
2. Склонируйте репозиторий: `git clone https://github.com/IKazarbekov/KnoTl_V2.git`
3. Перейдите в папку: `cd KnoTl_V2`
4. Запустите: `docker built -t backend_KnotlV2`
5. Приложение будет доступно по адресу `http://localhost:5000`

## Локальный запуск без Docker
1. Создайте виртуальное окружение: `python -m venv venv` и активируйте его.
2. Установите зависимости: `pip install -r requirements.txt`
3. Настройте переменные окружения (см. `.env.example`).
4. Выполните миграции: `flask db upgrade`
5. Запустите сервер: `flask run`

## Тестирование
Для запуска тестов выполните: `pytest`

## Структура проекта
- `app/` — основные модули приложения (маршруты, модели)
- `core/` — конфигурации, утилиты
- `tests/` — юнит-тесты

## Планы по развитию
- Добавить асинхронные задачи через Celery
- Интегрировать FastAPI для части API

## Контакты
Имя Фамилия | [hh.ru](...) | Telegram: @...
